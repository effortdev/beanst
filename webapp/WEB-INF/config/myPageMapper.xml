<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
	<entry key="selectMyInfo">
		SELECT * FROM users WHERE user_id = ?
	</entry>

	<entry key="checkMyPassword">
		SELECT COUNT(*) FROM users WHERE user_id = ? AND password = ?
	</entry>

	<entry key="checkEmailDup">
		SELECT COUNT(*) FROM users WHERE email = ? AND user_id != ?
	</entry>

	<entry key="checkPhoneDup">
		SELECT COUNT(*) FROM users WHERE phone = ? AND user_id != ?
	</entry>

	<entry key="updateMyInfo">
		UPDATE users SET name = ?, email = ?, phone = ? WHERE user_id = ?
	</entry>

	<entry key="updateMyPassword">
		UPDATE users SET password = ? WHERE user_id = ?
	</entry>

	<entry key="withdrawMyStatus">
		UPDATE users SET status = ? WHERE user_id = ?
	</entry>
</properties>